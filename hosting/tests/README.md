# HOSTING tests

HOSTING-specific contract and security checks are added incrementally with H2/H3.

H1 CI verifies the hosting frontend build and required package skeleton. Candidate submission must remain unavailable in the HOSTING API until the separately approved future Candidate Lite slice.
