# HOSTING database layer

H1 intentionally contains no production MySQL schema yet.

H2 will add the reviewed schema/migrations for:

- predefined pages;
- news;
- materials (`book`, `video`, `audio`, `article`);
- events;
- Lite Editor account/bootstrap data.

Candidate tables are explicitly excluded until the separately approved future Candidate Lite slice.
